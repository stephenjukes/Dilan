using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Classes.EncapsulationMissile
{
    class Missile
    {

    }
}

// Compare encapsulation to asking classes for information
// reading and writing public fields (show the pitfalls of this with missile example)
// Retaining full access
    // private fields - method access (using 'value' as set parameter)
    // expanded properties
    // note the private field go faint and use 'potential fix' to auto property
    // use SharpLab to show that the auto property retains the private field
// Limiting read write access with 
    // methods
    // expanded properties
    // shortcut properties
    // introduce readonly (for fields)
// Introducing conditions with 
    // expanded properties (limited to only the property value)
    // methods (including other parameters)
    // (shortcut properties have no logic)
